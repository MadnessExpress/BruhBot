module Play

  extend Discordrb::Commands::CommandContainer

  command(:play, min_args: 2, description: 'Invite groups to play a game.', usage: 'play <gamename>; <groupname>') do |event, *text, user|

    gamename = text.split(',')
    #text.split(',')

    user = event.user.username

    #playmessage = ["Hey #{groupname}, #{user} wants to play #{gamename}", "Hey #{groupname}, come play #{gamename} with #{user}"]

    event.respond gamename

  end

end
